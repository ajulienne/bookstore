{
  "name": "bookstore",
  "version": "1.0.0",
  "description": "A simple bookstore webapp made with differents JS framework and libraries",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "json": "json-server ./db.json -w -p 4000"
  },
  "author": "Antoine Julienne",
  "license": "MIT",
  "devDependencies": {
    "json-server": "^0.16.1"
  }
}
